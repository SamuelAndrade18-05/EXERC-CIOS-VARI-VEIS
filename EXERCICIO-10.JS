let a;
let b;
let c;
let x;
const delta = b ** 2 - 4 * a * c
const bhaskara = a * (Math.pow(x, 2)) + b * x + c


console.log(`Dados os números ${a}, ${b}, e ${c},  a fórmula de Bharkara resulta em ${bhaskara} e o valor do delta é ${delta}.`);
